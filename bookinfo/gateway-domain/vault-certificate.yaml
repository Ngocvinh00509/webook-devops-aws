apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-cert
  namespace: default
spec:
  secretName: vault-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: "vault.dnvinh99.xyz"
  dnsNames:
  - "vault.dnvinh99.xyz"
